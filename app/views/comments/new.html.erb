<h2>New comment</h2> 
<% form_for([@post, @comment]) do |f| %>  
	<%= f.error_messages %>  
	<p>  
		<%= f.label :body %><br />  
		<%= f.text_area :body, :size => "44x2"%>  
	</p>  
	
	<p>  
		<%= f.submit "Create" %>  
	</p> 
<% end %> 
<%= link_to 'Back', posts_path %> 